import {GET_GENRES} from "../Actions/types";

const menuReducers = (state = [], action) => {
    switch(action.type){               
        default:
            return state;
    }
}

export default menuReducers;